[build]
  command = "pnpm --filter @workspace/salelooterz run build"
  publish = "artifacts/salelooterz/dist/public"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH = "/"
  PORT = "3000"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
