kind = "web"
previewPath = "/"
title = "Salelooterz"
version = "1.0.0"
id = "artifacts/salelooterz"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20593

[services.development]
run = "pnpm --filter @workspace/salelooterz run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/salelooterz", "run", "build" ]
publicDir = "artifacts/salelooterz/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20593"
BASE_PATH = "/"
